# web port
server.port=8081

# log config
logging.config=classpath:logback.xml

# xxl-rpc
xxl-rpc.registry.xxlregistry.address=http://localhost:8080/xxl-rpc-admin
xxl-rpc.registry.xxlregistry.env=default